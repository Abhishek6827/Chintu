# Testing

- Currently, there is no automated testing framework (e.g., Jest, Cypress, Playwright) configured in the repository.
- Testing is entirely manual.
- Due to the dual-nature of the app (Next.js server vs Express server), developers must manually verify features in both `npm run dev` and the packaged `.exe` to ensure parity.
